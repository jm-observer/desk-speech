/node_modules/
/target/
/dist/
/src-tauri/target/
/src-tauri/gen/
assets
.idea
src-tauri/target
tmp
/src-tauri/sherpa-gpu/

# Rust build artifacts under server/ (e.g. orchestrator/target, ~234M)
server/**/target/

# TTS bake-off: vendored third-party GPT-SoVITS source is NOT committed.
# Deploy uses the pre-built gptsovits:bakeoff image; only a rebuild needs
# this dir present in the build context (see server/tts/README.md).
server/tts/GPT-SoVITS/

# Claude Code harness state (worktree links, local settings)
.claude/

# Python bytecode caches
**/__pycache__/
*.pyc

# TTS bake-off scratch: generated audio is ignored,
# gen/syn scripts + ref wavs + manifests are tracked
server/tts/tts-voices/outputs/
# sync_voices.py 的部署暂存区（合并后的 voices.json + wav 副本，产物不入 git）
server/tts/tts-voices/_deploy/
