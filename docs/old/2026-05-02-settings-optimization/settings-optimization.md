# 识别参数设置加载优化

| 章节       | 说明 |
|-----------|------|
| 时间       | 2026-05-02 |
| 项目现状    | 设置弹窗在打开时，会等待所有配置（包括远程 LLM 模型列表）加载完成后才显示 UI。如果网络延迟高，用户会长时间看到“正在加载中...”。 |
| 整体目标    | 提高设置弹窗的响应速度，实现配置先出、模型后出。 |
| Plan 1: 前端组件逻辑优化（解耦加载状态） | 已完成 |
| Plan 2: 后端缓存策略优化（可选） | 待开始 |
| 风险与待定项 | 如果用户在模型列表加载完之前切换到 LLM 标签页，需要显示局部的加载状态。 |
